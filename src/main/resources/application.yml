spring:
  application:
    name: springDockerEc2
  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    defer-datasource-initialization: true #script ??? hibernate ??? ?? ???? ?? ?? ??

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:board;DB_CLOSE_DELAY=-1
    username: sa
    password: